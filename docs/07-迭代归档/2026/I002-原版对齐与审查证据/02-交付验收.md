# 0.1.1 交付验收

更新时间：2026-09-09 21:54（Asia/Shanghai）

## 结果

- `npm run check` 通过：严格类型、20 项测试、19 份原版素材、15 种风味、提示词 6477–11366 字节和文档链接校验。
- `npm pack` 生成本地 `michengai-dsh-pua-0.1.1.tgz`，未发布到外部仓库。
- 已使用 `dsh plugin --profile web add <0.1.1 本地包> --ignore-scripts --registry=https://registry.npmjs.org/` 更新本机 web profile，安装成功。
- `dsh --profile web --dump-config` 可解析并包含 `michengai-pua`；只提取该插件条目，没有输出完整配置。
- 从实际安装目录解析宿主依赖，在临时内存会话检查误输入、原版模板、华为风味、只读审查、dsh-btw 仓库真实 Git 预检、关闭与卸载，全部通过。没有发送模型请求或修改用户会话。
- 本机宿主 Session 对头部校验更严格，首次安装检查脚本缺少 isSeeded 而失败；补全测试头部字段后通过，插件业务实现无需改动。
- 安装器仍有 profile peer 警告；本插件实际解析及注册成功，其他插件直接依赖声明与升级前一致。不代表整个 profile 的依赖兼容性已审计。

## 备份与恢复

升级前配置与依赖清单备份：用户目录 `.dsh/backups/dsh-pua-upgrade-20260909-215341`。包含 package.json、pnpm-lock.yaml、cordis.yml、cordis.patch.yml、pnpm-workspace.yaml，不包含完整 node_modules。

保留 0.1.0 安装包。如需恢复旧版，使用同一 `dsh plugin --profile web add` 命令安装项目中的 `michengai-dsh-pua-0.1.0.tgz`，然后按宿主要求重载后端。不要将配置清单备份当作完整依赖回滚副本。

未强制重启当前桌面或中断用户任务。等待当前任务结束后重载后端，再输入 `/pua help` 确认含 review；用 `/pua review` 复测同一项目，随后 `/pua evidence` 检查证据。若需关闭，输入 `/pua off`。

## 验证边界

本轮证明命令行为、提示词/模板投递、Git 证据采集与本机加载正确。尚未使用真实模型运行 0.1.1，因此不能宣称风味保持、发现准确率或效率已经提升。真实模型复测、跨平台安装和发布前许可复核由交接入口待办承接。

原先两个隔离安装目录保留，未再次尝试此前被自动审批拒绝的清理；本轮真实 Git 测试自建临时仓库在测试后已清理。
