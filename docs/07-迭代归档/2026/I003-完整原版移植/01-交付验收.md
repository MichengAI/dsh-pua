# 0.2.0 交付验收

更新时间：2026-09-09 23:09（Asia/Shanghai）

## 代码与行为

- `npm run check`：类型检查、38项测试和素材/提示词/文档校验通过。
- 104份素材，15种风味；11模式加auto的组合完整渲染，提示词52461–101455字节。
- 原文保真测试检查整个核心和对应模式正文，不仅检查关键词。
- 实际AgentLoop固定回复验证Oracle失败后继续、通过后结束、honor system、上限、暂停恢复、取消及卸载。
- 真实PowerShell验证exit0、exit7、命令不存在、超时；真实Git验证索引与本地忽略文件的区别。
- Session发布时禁止重入append；异常turn/end自身作为取消事实，修正后相关回归通过；取消后撤去Loop提示词，只有最新排队Loop能启动。

## 本地安装

目标：本机 `C:\Users\YUJIYU\.dsh\profiles\web`。

初次包 `michengai-dsh-pua-0.2.0.tgz` 已通过DSH安装器更新。最终交付包使用 `michengai-dsh-pua-0.2.0-local.tgz`，纳入最终归档与生命周期修正，使用新路径避免同版本缓存。安装命令均使用 `--ignore-scripts --registry=https://registry.npmjs.org/`。

`node scripts\smoke-package.mjs <web profile> <本仓库>` 从实际安装位置解析依赖，在临时内存会话验证完整核心、P9、auto、settings schema、完整资料读取、Git预检、开关及卸载。不读取或修改用户会话，不调用真实模型。最终包安装后执行同一检查。

安装器显示profile peer警告；本插件实际解析和注册检查通过，未将其描述为整个profile兼容性审计。

## 备份与回退

配置清单备份：`C:\Users\YUJIYU\.dsh\backups\dsh-pua-0.2.0-20260909-230356`，不包含完整node_modules。

保留0.1.1-final旧包。需要回退时重新安装该包并按宿主要求重载后端；0.1.1不理解新增模式和运行记录，建议回退后新开会话。

未强制重启后端或中断用户任务。用户等待现有任务结束后重载，以 `/pua help` 是否出现P9/Loop确认新版生效。可以用 `/pua flavor auto` 与 `/pua review` 复测。

## 验证边界

代码、固定原文、服务接线和本地安装已验证。真实模型的Banner/旁白保持、角色执行质量、完成率及耗时尚未对照评测；设置schema可发现不等于已完成桌面视觉验收。

旧两个隔离目录沿用历史保留，本轮未再次尝试此前被拒绝的删除。未推送或公开发布。
