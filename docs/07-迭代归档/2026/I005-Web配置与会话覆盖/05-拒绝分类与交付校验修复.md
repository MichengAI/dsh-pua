# 拒绝分类与交付校验修复

更新时间：2026-09-12 23:47（Asia/Shanghai）

## 实现

- 使用真实 tools.guard 物化 error.message 拒绝结果，确认 0.3.7 误判；修复后拒绝既不累计失败，也不清零已有观察。裸 Error 属未分类错误，不作为终端成功。带结构化错误码或直接非零退出字段仍按既有规则判断。
- 会话保存调用 parsePatch 后才调用 Remote，避免网关错误取代中文字段校验。浏览器实际会话面板键盘清空超时字段、Tab 失焦后出现中文错误，并保留空白草稿；此为组件验证，不等于真实网关端到端验收。
- esbuild 生成最终 client.js.map，并写入 sourceMappingURL；回归确认包含客户端与配置模块映射。
- 兼容树移至系统临时目录，清除 NODE_PATH，核对直接运行依赖路径必须位于隔离树内。

## 验证与交付

真实 guard 回归已先失败、修复后通过；压缩生命周期夹具改为显式非零退出码，保留全部恢复断言，另增裸错误不计数断言。npm run check：93/93。仓库外 npm run test:compat：rc.2 与 rc.1 均 93/93，0.1.2-rc.1 为 90/90。素材、提示词和文档链接检查通过。0.3.8 本机 web profile 安装冒烟与 client/runtime/map 字节核对通过；未重启后端，真实桌面效果待用户确认。

代码与文档基线分别为 67c15b3、94c3b9d。本轮代码提交：`b621893`；配套文档随本次 docs 提交归档。未推送、未发布，安装状态见[当前状态](../../../00-交接入口/02-当前状态.md)。
